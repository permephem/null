{
  "name": "null-protocol-subgraph",
  "version": "1.0.0",
  "description": "The Graph subgraph for Null Protocol CanonRegistry events",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ null-protocol/canon-registry",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 null-protocol/canon-registry",
    "create-local": "graph create --node http://localhost:8020/ null-protocol/canon-registry",
    "remove-local": "graph remove --node http://localhost:8020/ null-protocol/canon-registry",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.56.0",
    "@graphprotocol/graph-ts": "0.31.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.2"
  }
}
